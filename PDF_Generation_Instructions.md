# PDF Generation Instructions

To generate a PDF from your `README.md` file, use the following command in your terminal:

```bash
mdpdf README.md -o README.pdf
```

This command will convert the `README.md` file into a PDF named `README.pdf` in the same directory.
